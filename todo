server:
1. Сделать хэширование
2. Добавить/исправить логи (чтобы не копировалась конф.инфа (как, например, UserService.register))
3. Добавить транзакции (например, UserService.register - чтобы, если возникнет ошибка при сохранении фоток, не было сохранения записи о пользователе)
4. Поработать с SOLID (почему UserService знает о jwt, может быть добавить ещё middleware - сервисов?)
5. убрать alert`ы, использованные для обработки ошибок (в приницпе поработать с обработкой возможных ошибок)
client:
1. поработать с безопасностью клиента (например, не хранить jwt в localstorage)
2. обработка ошибок
3. архитектура (нет единго подхода к загрузке данных, обработке ошибок + поработать над страицами и их компонентами)